let multiplicando = +process.argv[2];
let multiplicador = 1;
let resultado = [];
while(multiplicador <= 10){
    let producto = multiplicando * multiplicador;
    console.table("Multiplicando * Multiplicador = Producto")
    console.table(`${multiplicando} * ${multiplicador} = ${producto}`);
    multiplicador++;
}

while(multiplicador <= 1){
    resultado.push{(
        multiplicando = multiplicando,
        multiplicador  = multiplicador,
        
    )
    }
}